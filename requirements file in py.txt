actually we dont need to upload all the files from our venv, because it is not efficient
so in order to make this efficient we could use the next

pip freeze > requirements.txt

and when somebody will want to use our repo from github they will just do

pip install -r requirements.txt